﻿@model IEnumerable<FitFeastExplore.Models.WorkOutDto>
@{
    ViewBag.Title = "List WorkOuts";
}

<h2>List of WorkOuts</h2>

<table class="table">
    <tr>
        <th>WorkOut Name</th>
        <th></th>
        <th></th>
        <th></th>
    </tr>
    @foreach (var item in Model)
    {
        <tr>
            <td>@item.Name</td>
            <td>
                <a href="@Url.Action("Details", "WorkOut", new { id = item.WorkOutId })">Details</a>
            </td>
            <td>
                <a href="@Url.Action("Edit", "WorkOut", new { id = item.WorkOutId })">Edit</a>
            </td>
            <td>
                <a href="@Url.Action("Delete", "WorkOut", new { id = item.WorkOutId })">Delete</a>
            </td>
        </tr>
    }
</table>